
function Update()
	
	-- DebugLog("Hello from Lua!");
	
	-- local dt = GetDeltaTime();
	-- DebugLog("Delta time is: " .. dt);

	-- local seconds = GetAppSeconds();
	-- DebugLog("App time is: " .. seconds);
	
	--DebugLog( "Hello from CoolScript.lua: " );

end

-- TODO List
-- DebugPrintScreen("Hello")
-- DrawDebugLine(Start, End, Color, Duration);
-- PhysicsRaycast();
-- DrawSphere(Location, Radius, Duration);
-- DrawBox(Location, Dimensions, Duration);
